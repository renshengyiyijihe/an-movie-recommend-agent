/** 用户名最短长度，与 auth-service RegisterDto 对齐。 */
export const AUTH_USERNAME_MIN_LENGTH = 2;

/** 用户名最长长度。 */
export const AUTH_USERNAME_MAX_LENGTH = 50;

/** 密码最短长度，与 LoginDto / RegisterDto 对齐。 */
export const AUTH_PASSWORD_MIN_LENGTH = 6;

/** 密码最长长度。 */
export const AUTH_PASSWORD_MAX_LENGTH = 128;
